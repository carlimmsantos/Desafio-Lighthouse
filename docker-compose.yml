services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: lh_nautical
    ports:
      - "5432:5432"
    volumes:
      - lh_nautical_pgdata:/var/lib/postgresql/data

volumes:
  lh_nautical_pgdata:
